---
interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<div class="not-prose mb-6 space-y-4">
  {title ? <h2 class="text-2xl font-bold">{title}</h2> : null}
  <div class="grid grid-cols-1 gap-4 md:grid-cols-2">
    <slot />
  </div>
</div>
